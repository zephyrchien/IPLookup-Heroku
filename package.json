{
	"type": "module",
	"name": "iplookup-heroku",
	"version": "0.9.0",
	"description": "Lookup IP",
	"main": "index.js",
	"scripts": {
		"format": "prettier --semi --use-tabs --write '**/*.{js,json,md}'",
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js"
	},
	"author": "zephyr",
	"license": "GPL-3.0",
	"dependencies": {
		"express": "^4.17.1",
		"node-ip2region": "^1.0.2",
		"prettier": "^2.2.1"
	}
}
